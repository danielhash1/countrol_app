<h1>Categories</h1>

<%= link_to "Add a Category", new_category_path %>

<ul>
  <% @categories.each do |category| %>
  <li><%= category.icon %> <%= link_to category.name, category_path(category) %></li>
  <% end %>
</ul>
