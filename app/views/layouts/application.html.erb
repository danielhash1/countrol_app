<!DOCTYPE html>
<html>
  <head>
    <title>Countrol</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>

  <div class="page-container">

    <% if controller_name == "wallets" && action_name == "show" %>
    <div class="header-container"><%= render "shared/header" %></div>
    <% elsif controller_name == "transactions" %>
    <div class="header-container"><%= render "shared/header" %></div>

    <% end %>

    <div class="content-container"><%= yield %></div>

    <% if controller_name == "wallets" && action_name == "show" %>
      <div class="navbar-container"><%= render "shared/navbar" %></div>
    <% elsif controller_name == "transactions" %>
      <div class="header-container"><%= render "shared/navbar" %></div>


    <% end %>
  </div>
  </body>
</html>
